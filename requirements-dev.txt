# Test dependencies — not included in the production Docker image
pytest==8.3.5
pytest-asyncio==0.24.0
httpx==0.27.0         # starlette TestClient uses httpx under the hood

# Linter
ruff==0.8.6

# Schema migrations (optional — app auto-creates tables via create_all in dev/test)
alembic==1.14.0
