# Helm values applied when deploying via Zarf.
# ###ZARF_VAR_x### tokens are replaced by Zarf at deploy time.
config:
  offlineMode: ###ZARF_VAR_OFFLINE_MODE###
  nasaApiKey: "###ZARF_VAR_NASA_API_KEY###"

udsPackage:
  enabled: ###ZARF_VAR_UDS_PACKAGE_ENABLED###
  host: "###ZARF_VAR_HOST###"
