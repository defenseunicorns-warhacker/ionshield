apiVersion: v1
kind: Service
metadata:
  name: ionshield
  labels:
    {{- include "ionshield.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "ionshield.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
